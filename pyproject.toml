[project]
name = "flaskgpt"
version = "0.1.0"
description = "ChatGPT clone usung flask"
authors = [
    {name = "jerome-byte",email = "akotayaojerome@gmail.com"}
]
requires-python = ">=3.14"
dependencies = [
    "flask (==2.3.3)",
    "python-dotenv (==1.0.0)",
    "google-generativeai (>=0.8.6,<0.9.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
